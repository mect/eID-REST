# Belgian eID REST API

This project reads a Belgian eID card and returns the data in JSON format.
It is using OpenSC under the hood, and currently supports only Belgian eID cards and will only run on Linux.
